# 🧪 Тестирование Description Highlighting v2.0

**Дата:** 2025-11-20
**Версия:** 2.0

---

## Быстрый старт

### 1. Запустить приложение
```bash
cd frontend
npm run dev
```

### 2. Открыть книгу с описаниями
```
http://localhost:3000/reader/{book_id}
```

### 3. Открыть консоль браузера (F12)

### 4. Искать в логах:
```
🎨 [SUMMARY] Highlighting complete
```

---

## ✅ Что проверять

### 1. Coverage (Покрытие)

**Ожидание:**
```
coverage: "95-100%"
```

**Проверка:**
- Визуально: все описания подсвечены голубым
- Console: `highlighted: X, total: Y` → X ≈ Y

**Если coverage < 95%:**
```
⚠️ [FAILED DESCRIPTIONS] N not highlighted:
  - #12: too_short_after_cleanup - "..."
  - #34: no_match_in_dom - "..."
```
→ Смотрите причины и примеры текста

---

### 2. Performance (Производительность)

**Ожидание:**
```
duration: "<100ms"
```

**Проверка:**
- Console: `duration: "45.23ms"` (должно быть <100ms)

**Если duration > 100ms:**
```
⚠️ [PERFORMANCE] Highlighting took 145.67ms (target: <100ms)
```
→ Возможно, слишком много описаний или длинные тексты

---

### 3. Strategies (Стратегии)

**Проверка:**
Смотрите в логах какие стратегии используются:
```
✅ [S1_First_40] Highlighted #1: "..."
✅ [S2_Skip_10] Highlighted #5: "..."
✅ [S4_Full_Match] Highlighted #12: "..."
✅ [S5_Fuzzy_5_Words] Highlighted #34: "..."
```

**Распределение (идеально):**
- S1 (First 40): ~40%
- S2 (Skip 10): ~20%
- S3 (Skip 20): ~10%
- S4 (Full match): ~15%
- S5 (Fuzzy): ~10%
- S6 (CFI): ~5%

---

### 4. Debounce (Отклик)

**Проверка:**
1. Быстро перелистывать страницы (Next → Next → Next)
2. Смотреть логи:
```
📄 [useDescriptionHighlighting] Page rendered, scheduling highlights...
📄 [useDescriptionHighlighting] Debounce complete, applying highlights...
```

**Ожидание:**
- Задержка ~100ms между событиями
- Нет дублирующихся вызовов

---

### 5. Visual (Визуальная проверка)

**Ожидание:**
- ✅ Описания подсвечены голубым (`rgba(96, 165, 250, 0.2)`)
- ✅ Hover эффект (цвет темнеет до `0.3`)
- ✅ Синяя линия снизу (`border-bottom: 2px solid #60a5fa`)
- ✅ Курсор pointer
- ✅ Клик открывает модальное окно с изображением

---

## 🧪 Test Cases

### Test Case 1: Обычные описания (S1)
**Описание:** "Темный лес окутал горизонт, деревья возвышались..."
**Ожидание:** ✅ Подсвечено через S1_First_40

### Test Case 2: Описания с префиксом (S2)
**Описание:** "В главе первой. Старый замок стоял на холме..."
**Ожидание:** ✅ Подсвечено через S2_Skip_10 (обход "В главе первой.")

### Test Case 3: Описания с chapter header (removeChapterHeaders)
**Описание:** "Глава первая. Темный коридор простирался..."
**Ожидание:** ✅ "Глава первая." удалена, подсвечено "Темный коридор..."

### Test Case 4: Короткие описания (S4)
**Описание:** "Красное платье" (14 символов)
**Ожидание:** ✅ Подсвечено через S4_Full_Match

### Test Case 5: Non-breaking spaces (normalizeText)
**Описание:** "Старый\u00A0дом\u00A0на\u00A0холме"
**Ожидание:** ✅ Подсвечено (нормализация \u00A0 → ' ')

### Test Case 6: Кавычки (normalizeText)
**Описание:** "«Привет», — сказал он"
**Ожидание:** ✅ Подсвечено (« и » нормализованы)

### Test Case 7: Быстрая навигация (debounce)
**Действие:** Next → Next → Next (быстро)
**Ожидание:** ✅ Debounce отменяет предыдущие вызовы, только последний выполняется

---

## 📊 Сбор метрик

### 1. Coverage metrics
```javascript
// В console:
const highlights = document.querySelectorAll('.description-highlight');
console.log(`Highlights: ${highlights.length}`);
console.log(`Strategies:`, [...highlights].map(h => h.dataset.strategy));
```

**Ожидание:**
```
Highlights: 115 (или близко к total descriptions)
Strategies: ['S1_First_40', 'S2_Skip_10', 'S4_Full_Match', ...]
```

### 2. Performance metrics
```javascript
// Собрать из логов:
const durations = []; // Из "duration: XX.XXms"
const avgDuration = durations.reduce((a, b) => a + b) / durations.length;
console.log(`Average duration: ${avgDuration.toFixed(2)}ms`);
```

**Ожидание:**
```
Average duration: <100ms
```

---

## 🐛 Troubleshooting

### Проблема: Coverage < 95%

**Возможные причины:**
1. Описания слишком короткие (<10 символов)
   - Решение: Проверить `failedDescriptions` → `reason: 'too_short'`

2. Описания не найдены в DOM
   - Решение: Проверить `reason: 'no_match_in_dom'`
   - Возможно, текст в EPUB отличается от description.content

3. Chapter headers не удалены
   - Решение: Проверить `removeChapterHeaders()` регулярные выражения

**Действия:**
```javascript
// В console:
// Проверить failed descriptions
console.warn(`⚠️ [FAILED DESCRIPTIONS]`);
// Скопировать примеры текстов
// Сравнить с EPUB содержимым
```

---

### Проблема: Duration > 100ms

**Возможные причины:**
1. Слишком много описаний (>200)
   - Решение: Ожидаемо, это нормально

2. Длинные описания (>200 символов)
   - Решение: Strategy 4 пропускает их (by design)

3. Медленный DOM manipulation
   - Решение: Проверить количество DOM операций

**Действия:**
```javascript
// В console:
performance.mark('start');
// ... highlighting ...
performance.mark('end');
performance.measure('highlighting', 'start', 'end');
console.log(performance.getEntriesByName('highlighting'));
```

---

## 📝 Отчет после тестирования

### Шаблон отчета:
```markdown
# Отчет тестирования Description Highlighting v2.0

**Дата:** YYYY-MM-DD
**Книга:** {book_title} ({book_id})

## Метрики:
- **Coverage:** X% (Y из Z описаний)
- **Performance:** XX.XXms average
- **Failed:** N описаний

## Стратегии:
- S1: XX%
- S2: XX%
- S3: XX%
- S4: XX%
- S5: XX%
- S6: XX%

## Failed Descriptions:
1. #X: reason - "preview..."
2. #Y: reason - "preview..."

## Визуальная проверка:
- [ ] Подсветка работает
- [ ] Hover эффект
- [ ] Клик открывает изображение
- [ ] Нет дублирующихся highlights

## Performance:
- [ ] <100ms на главу
- [ ] Debounce работает
- [ ] Нет memory leaks

## Выводы:
...
```

---

## ✅ Checklist тестирования

### Функциональность:
- [ ] Подсветка работает (все 6 стратегий)
- [ ] Coverage ≥95%
- [ ] Нормализация текста работает
- [ ] Chapter headers удаляются
- [ ] Debounce работает

### Performance:
- [ ] Duration <100ms
- [ ] Нет warnings в console
- [ ] Быстрая навигация без lag

### UX:
- [ ] Hover эффект
- [ ] Клик открывает модальное окно
- [ ] Визуально приятно
- [ ] Нет перекрытий текста

### Debugging:
- [ ] Логи детализированы
- [ ] Failed descriptions видны
- [ ] Стратегии отслеживаются

---

**Автор:** Frontend Developer Agent
**Дата:** 2025-11-20
**Версия:** 2.0

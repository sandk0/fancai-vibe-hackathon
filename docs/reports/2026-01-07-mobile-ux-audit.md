# Аудит мобильной версии приложения fancai

**Дата:** 7 января 2026
**Версия:** 1.0
**Статус:** Требует доработки

---

## Содержание

1. [Резюме](#резюме)
2. [Критические проблемы](#критические-проблемы)
3. [Проблемы навигации](#проблемы-навигации)
4. [Проблемы главной страницы](#проблемы-главной-страницы)
5. [Проблемы адаптивности страниц](#проблемы-адаптивности-страниц)
6. [Проблемы стилей и дизайн-системы](#проблемы-стилей-и-дизайн-системы)
7. [План доработки](#план-доработки)

---

## Резюме

### Общая оценка: 6.5/10

**Версия аудита:** 2.0 (дополнено 7 января 2026)

### КРИТИЧЕСКАЯ АРХИТЕКТУРНАЯ ПРОБЛЕМА

**Двойной контейнер в Layout + Pages:**

1. **Layout.tsx** добавляет `container mx-auto px-4` (строка 62)
2. **HomePage.tsx** добавляет `max-w-7xl mx-auto px-4` (строка 716)
3. **LibraryPage.tsx** и другие страницы имеют свои контейнеры

**Результат:** Вложенные контейнеры с накопленными отступами.

**tailwind.config.js проблема:**
```js
container: {
  center: true,
  padding: "2rem",      // 32px padding!
  screens: {
    "2xl": "1400px",    // max-width ТОЛЬКО на 2xl!
  },
}
```

Контейнер не имеет breakpoints для sm, md, lg, xl — на этих экранах max-width = 100%, что может вызывать проблемы с layout

Приложение имеет **хорошую базовую мобильную адаптацию** с правильным использованием:
- ✅ Responsive breakpoints (sm:, md:, lg:, xl:)
- ✅ Safe-area поддержка для iOS (notch, home indicator)
- ✅ Touch targets минимум 44x44px
- ✅ Предотвращение zoom на input (16px font-size)
- ✅ Темы с хорошим контрастом (WCAG AA/AAA)

Однако выявлены **критические проблемы**:
- 🔴 Hover-only interactions в ImageGallery недоступны на мобилях
- 🔴 Несогласованность логики активного состояния навигации
- 🔴 Горизонтальный скролл может вызывать overflow на главной
- 🔴 Dropdown меню могут выходить за границы экрана

---

## Критические проблемы

### 1. ImageGallery: hover-only actions недоступны на мобилях

**Файл:** `src/components/Images/ImageGallery.tsx`
**Строки:** 238, 244

```tsx
// ПРОБЛЕМА: Кнопки Download/Share видны только при hover
<div className="opacity-0 group-hover:opacity-100 ...">
  <Button>Download</Button>
  <Button>Share</Button>
</div>
```

**Последствия:** На мобильных устройствах пользователь не может скачать или поделиться изображением — кнопки полностью невидимы.

**Решение:** Добавить альтернативный UI для мобилей (long-press меню или всегда видимые кнопки).

---

### 2. Несогласованность логики навигации

| Компонент | Логика | Файл | Строка |
|-----------|--------|------|--------|
| BottomNav | `startsWith()` | BottomNav.tsx | 32-36 |
| MobileDrawer | `===` (точное) | MobileDrawer.tsx | - |
| Header | `startsWith()` | Header.tsx | - |
| Sidebar | `===` (точное) | Sidebar.tsx | 117 |

**Последствия:** При URL `/library/123`:
- BottomNav подсветит "Библиотека" ✅
- MobileDrawer не подсветит ничего ❌

**Решение:** Унифицировать логику во всех компонентах.

---

### 3. Admin-панель отсутствует в BottomNav

**Файл:** `src/components/Navigation/BottomNav.tsx`
**Строки:** 10-16

```tsx
// ПРОБЛЕМА: Статический список без админ-панели
const navItems: NavItem[] = [
  { path: '/', label: 'Главная', icon: Home },
  { path: '/library', label: 'Библиотека', icon: Library },
  // ... НЕТ /admin для админов!
];
```

**Решение:** Добавить условный рендеринг как в MobileDrawer.

---

### 4. Header dropdown выходит за экран

**Файл:** `src/components/Layout/Header.tsx`
**Строка:** 183

```tsx
// ПРОБЛЕМА: w-56 (224px) может выйти за край на экранах < 280px
<div className="w-56 right-0 ...">
```

**Решение:** Использовать `max-w-[calc(100vw-2rem)]` или fullscreen на мобилях.

---

### 5. Slider touch target критически мал

**Файл:** `src/components/UI/slider.tsx`
**Строка:** 21

```tsx
// КРИТИЧНО: Touch target 20×20px вместо минимальных 44×44px
<SliderPrimitive.Thumb className="block h-5 w-5 rounded-full..." />
```

**Требования:**
- Apple HIG: минимум 44×44pt
- Material Design 3: минимум 48×48dp
- WCAG 2.2 (AA): минимум 24×24px (рекомендуется 44×44px)
- MIT Touch Lab: ~10mm × 10mm

**Последствия:**
- 30%+ ошибок при касании
- Невозможность точной регулировки громкости/яркости
- Плохой UX для пользователей с крупными пальцами

**Решение:**
```tsx
<SliderPrimitive.Thumb className="block h-11 w-11 rounded-full..." />
// Или с расширенной областью касания:
<SliderPrimitive.Thumb className="relative h-5 w-5 rounded-full before:absolute before:-inset-3 before:content-['']" />
```

---

### 6. Modal close button слишком мал

**Файл:** `src/components/UI/Modal.tsx`
**Строка:** 349

```tsx
// ПРОБЛЕМА: 32×32px — ниже минимальных 44×44px
<button className="flex h-8 w-8 items-center justify-center rounded-md">
```

**Решение:**
```tsx
<button className="flex h-11 w-11 items-center justify-center rounded-md">
```

---

### 7. Dropdown items высота недостаточна

**Файл:** `src/components/UI/dropdown-menu.tsx`
**Строки:** 28, 84, 100, 124

```tsx
// ПРОБЛЕМА: py-1.5 даёт ~28px высоты вместо 44px
<DropdownMenuPrimitive.Item className="... py-1.5 ...">
```

**Последствия:** Трудно точно нажать на нужный пункт меню на мобилях.

**Решение:**
```tsx
// Добавить min-height
<DropdownMenuPrimitive.Item className="... py-1.5 min-h-[44px] ...">
```

---

## Аудит UI компонентов

### Сводная таблица touch targets

| Компонент | Файл | Текущий размер | Статус | Требуется |
|-----------|------|----------------|--------|-----------|
| **Slider thumb** | slider.tsx:21 | 20×20px | 🔴 КРИТИЧНО | 44×44px |
| **Modal close** | Modal.tsx:349 | 32×32px | 🔴 КРИТИЧНО | 44×44px |
| **Dropdown item** | dropdown-menu.tsx:84 | ~28px высота | 🟠 Проблема | 44px min-h |
| **Button (md)** | button.tsx:128 | 44px высота | ✅ ОК | — |
| **Button (icon)** | button.tsx:136 | 44×44px | ✅ ОК | — |
| **Button (sm)** | button.tsx:124 | 36px высота | 🟡 Внимание | 44px |
| **Input (md)** | Input.tsx:70 | 44px высота | ✅ ОК | — |
| **Input (sm)** | Input.tsx:66 | 36px высота | 🟡 Внимание | 44px |
| **Select (default)** | Select.tsx:34 | 44px высота | ✅ ОК | — |
| **Select (sm)** | Select.tsx:35 | 40px высота | 🟡 Внимание | 44px |

### Компоненты соответствующие стандартам

**Button (button.tsx):**
- ✅ Default size `h-11` = 44px (Apple HIG minimum)
- ✅ Icon size `h-11 w-11` = 44×44px
- ✅ Large size `h-12` = 48px
- ✅ `touch-action-manipulation` для предотвращения double-tap zoom
- 🟡 Small size `h-9` = 36px (ниже минимума, но документировано)

**Input (Input.tsx):**
- ✅ Default size `h-11` = 44px
- ✅ Font size `text-base` = 16px (предотвращает zoom на iOS)
- ✅ Large size `h-12` = 48px
- 🟡 Small size `h-9` = 36px

**Select (Select.tsx):**
- ✅ Native select → использует нативный picker на мобилях
- ✅ Default height 44px
- ✅ Chevron иконка для визуальной подсказки

**Modal (Modal.tsx):**
- ✅ Drawer variant для мобилей (bottom sheet)
- ✅ `max-h-[85vh]` предотвращает выход за экран
- ✅ Focus trap для доступности
- ✅ Close on Escape и backdrop click
- 🔴 Close button 32×32px (требует исправления)

---

## Проблемы навигации

### Отсутствие навигации в режиме чтения

**Файл:** `src/App.tsx`
**Строки:** 81-90

BookReaderPage рендерится **без Layout**, пользователь не может вернуться назад без системной кнопки браузера.

### Ложноположительные совпадения маршрутов

При использовании `startsWith()` URL `/image-editor` подсветит `/image` (если такой маршрут добавят).

---

## Проблемы главной страницы

### Hero Section: большие отступы

**Файл:** `src/pages/HomePage.tsx`
**Строка:** 105

```tsx
// ПРОБЛЕМА: py-12 (48px) много для экранов < 360px
<div className="relative px-6 py-12 sm:px-10 sm:py-16 lg:py-24">
```

**Рекомендация:**
```tsx
<div className="relative px-4 sm:px-6 md:px-10 py-8 sm:py-12 md:py-16 lg:py-24">
```

### Горизонтальный скролл секции "Недавно добавленные"

**Файл:** `src/pages/HomePage.tsx`
**Строки:** 422-425

```tsx
// ПРОБЛЕМА: -mx-4 px-4 может создать скрытый overflow
className="flex gap-4 overflow-x-auto pb-4 -mx-4 px-4 sm:mx-0 sm:px-0"
```

**Проблемы:**
- Нет визуальной подсказки о горизонтальном скролле
- Кнопки прокрутки скрыты на мобилях (`sm:flex`)

**Рекомендация:** Добавить градиент справа для индикации скролла.

### Statistics grid не оптимален

**Файл:** `src/pages/HomePage.tsx`
**Строка:** 579

```tsx
// ПРОБЛЕМА: На планшетах (768px) всё ещё 2 колонки
<div className="grid grid-cols-2 lg:grid-cols-4 gap-3 sm:gap-4">
```

**Рекомендация:**
```tsx
<div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2 sm:gap-3 md:gap-4">
```

---

## Проблемы адаптивности страниц

### Сводная таблица по страницам

| Страница | Проблема | Серьёзность |
|----------|----------|-------------|
| **ProfilePage** | Avatar 128px слишком большой на мобилях | Средняя |
| **ProfilePage** | Email без truncate может переполнить | Низкая |
| **SettingsPage** | Боковая навигация не скрыта на планшетах | Средняя |
| **SettingsPage** | Toggle switch 44x24px может быть мал | Низкая |
| **BookPage** | Grid 3 колонки статистики тесный на 320px | Средняя |
| **ChapterPage** | Не применены safe-area-insets | Средняя |
| **RegisterPage** | Критерии пароля в 2 колонки тесные | Средняя |
| **AdminDashboard** | Табы могут переполнить экран | Средняя |
| **BookImagesPage** | Обложка 160x224px велика для портрета | Средняя |
| **NotFoundPage** | Текст 9xl может выйти за край < 375px | Средняя |

### Хорошие примеры (для справки)

- **LoginPage** — правильное использование safe-area-insets
- **RegisterPage** — корректный responsive padding
- **BottomNav** — идеальная мобильная реализация

---

## Проблемы стилей и дизайн-системы

### Z-index нарушение в Layout.tsx

**Файл:** `src/components/Layout/Layout.tsx`
**Строка:** 51

```tsx
// ПРОБЛЕМА: z-30 не соответствует масштабу (должно быть z-[400])
<div className="z-30 fixed inset-0 bg-black/50" />
```

### BottomNav: слишком маленький текст

**Файл:** `src/components/Navigation/BottomNav.tsx`
**Строка:** 82

```tsx
// ПРОБЛЕМА: 10px текст сложно читать
<span className="text-[10px]">{item.label}</span>
```

**Рекомендация:** Минимум 12px для читаемости.

### Отсутствие стилей для экранов < 360px

Нет медиа-запросов для очень маленьких экранов (iPhone SE 1st gen, бюджетные Android).

---

## План доработки

### Фаза 1: Критические исправления (P0)

| # | Задача | Файл | Оценка |
|---|--------|------|--------|
| 1.0 | **АРХИТЕКТУРА:** Убрать двойной контейнер | Layout.tsx, tailwind.config.js | 2ч |
| 1.1 | Добавить мобильный UI для ImageGallery actions | ImageGallery.tsx | 2ч |
| 1.2 | Унифицировать логику активного состояния навигации | BottomNav, MobileDrawer, Sidebar | 1ч |
| 1.3 | Добавить админ-панель в BottomNav для админов | BottomNav.tsx | 30м |
| 1.4 | Исправить overflow в Header dropdown | Header.tsx | 30м |
| 1.5 | Исправить z-index в Layout.tsx | Layout.tsx | 15м |
| 1.6 | **Slider thumb 44×44px** | slider.tsx | 30м |
| 1.7 | **Modal close button 44×44px** | Modal.tsx | 15м |
| 1.8 | **Dropdown items min-height 44px** | dropdown-menu.tsx | 30м |

**Итого Фаза 1:** ~7.5 часов

### Фаза 2: Важные улучшения (P1)

| # | Задача | Файл | Оценка |
|---|--------|------|--------|
| 2.1 | Добавить кнопку "Назад" в режим чтения | BookReaderPage.tsx | 1ч |
| 2.2 | Исправить горизонтальный скролл на HomePage | HomePage.tsx | 1ч |
| 2.3 | Оптимизировать отступы Hero section | HomePage.tsx | 30м |
| 2.4 | Увеличить размер текста в BottomNav до 12px | BottomNav.tsx | 15м |
| 2.5 | Добавить скрытие sidebar на планшетах в SettingsPage | SettingsPage.tsx | 1ч |
| 2.6 | Применить safe-area в ChapterPage | ChapterPage.tsx | 30м |

**Итого Фаза 2:** ~4.5 часа

### Фаза 3: UX улучшения (P2)

| # | Задача | Файл | Оценка |
|---|--------|------|--------|
| 3.1 | Добавить медиа-запросы для экранов < 360px | globals.css | 1ч |
| 3.2 | Оптимизировать grid статистики на HomePage | HomePage.tsx | 30м |
| 3.3 | Уменьшить размер avatar на ProfilePage для мобилей | ProfilePage.tsx | 15м |
| 3.4 | Добавить truncate для email на ProfilePage | ProfilePage.tsx | 15м |
| 3.5 | Оптимизировать grid критериев пароля | RegisterPage.tsx | 30м |
| 3.6 | Уменьшить обложку на BookImagesPage | BookImagesPage.tsx | 15м |
| 3.7 | Адаптировать текст 404 страницы | NotFoundPage.tsx | 15м |
| 3.8 | Добавить горизонтальный скролл для табов в AdminDashboard | AdminDashboard.tsx | 1ч |

**Итого Фаза 3:** ~4 часа

### Фаза 4: Оптимизация (P3)

| # | Задача | Файл | Оценка |
|---|--------|------|--------|
| 4.1 | Добавить "Outdoor mode" для чтения на солнце | ReaderSettingsPanel.tsx | 2ч |
| 4.2 | Оптимизировать animations с will-change | globals.css | 1ч |
| 4.3 | Добавить pointer-events для touch vs mouse | globals.css | 1ч |
| 4.4 | Использовать clamp() для font-sizes | globals.css | 1ч |

**Итого Фаза 4:** ~5 часов

---

## Общая оценка трудозатрат

| Фаза | Приоритет | Время | Статус |
|------|-----------|-------|--------|
| Фаза 1 | P0 (Критично) | ~7.5ч | ✅ **ВЫПОЛНЕНО** (7 января 2026) |
| Фаза 2 | P1 (Важно) | ~4.5ч | ✅ **ВЫПОЛНЕНО** (7 января 2026) |
| Фаза 3 | P2 (Улучшения) | ~4ч | ✅ **ВЫПОЛНЕНО** (7 января 2026) |
| Фаза 4 | P3 (Оптимизация) | ~5ч | ✅ **ВЫПОЛНЕНО** (7 января 2026) |
| **Итого** | | **~21 час** | **✅ ВСЁ ВЫПОЛНЕНО** |

### Выполненные исправления P0 (7 января 2026)

| # | Задача | Статус |
|---|--------|--------|
| 1.0 | Убрать двойной контейнер (Layout + tailwind.config) | ✅ |
| 1.1 | ImageGallery mobile UI (кнопки видны на touch) | ✅ |
| 1.2 | Унифицировать isActiveRoute | ✅ |
| 1.3 | Admin в BottomNav | ✅ |
| 1.4 | Header dropdown overflow | ✅ |
| 1.5 | z-index в Layout | ✅ |
| 1.6 | Slider thumb 44×44px | ✅ |
| 1.7 | Modal close 44×44px | ✅ |
| 1.8 | Dropdown items min-h 44px | ✅ |

### Выполненные исправления P1 (7 января 2026)

| # | Задача | Статус |
|---|--------|--------|
| 2.1 | Кнопка "Назад" в режиме чтения (ReaderHeader) | ✅ |
| 2.2 | Градиент-индикатор горизонтального скролла (HomePage) | ✅ |
| 2.3 | Оптимизация отступов Hero section | ✅ |
| 2.4 | Увеличение текста в BottomNav (11px/12px) | ✅ |
| 2.5 | Mobile tabs в SettingsPage (sidebar скрыт на < lg) | ✅ |
| 2.6 | Safe-area в ChapterPage (pt-safe pb-safe) | ✅ |

### Выполненные исправления P2 (7 января 2026)

| # | Задача | Статус |
|---|--------|--------|
| 3.1 | Медиа-запросы для экранов < 360px (globals.css) | ✅ |
| 3.2 | Оптимизация grid статистики на HomePage (3 колонки на sm) | ✅ |
| 3.3 | Уменьшение avatar на ProfilePage (96px на мобилях) | ✅ |
| 3.4 | Truncate для email на ProfilePage | ✅ |
| 3.5 | Оптимизация grid критериев пароля (1 колонка на xs) | ✅ |
| 3.6 | Уменьшение обложки на BookImagesPage (128×176px) | ✅ |
| 3.7 | Адаптивный текст 404 (text-7xl на мобилях) | ✅ |
| 3.8 | Горизонтальный скролл табов в AdminDashboard | ✅ |

### Выполненные исправления P3 (7 января 2026)

| # | Задача | Статус |
|---|--------|--------|
| 4.1 | Outdoor mode для чтения на солнце (5 файлов) | ✅ |
| 4.2 | Оптимизация animations с will-change | ✅ |
| 4.3 | Pointer-events для touch vs mouse | ✅ |
| 4.4 | Fluid typography с clamp() | ✅ |

---

## Приложения

### A. Скриншоты проблем

*Рекомендуется сделать скриншоты на реальных устройствах:*
- iPhone SE (375px) — минимальная ширина
- iPhone 14 Pro (393px) — с Dynamic Island
- Android (360px) — бюджетный сегмент

### B. Тестовые устройства

| Устройство | Ширина | Особенности |
|------------|--------|-------------|
| iPhone SE | 375px | Минимум для iOS |
| iPhone 14 | 390px | Dynamic Island |
| iPhone 14 Pro Max | 430px | Большой экран |
| Pixel 7 | 412px | Android стандарт |
| Samsung A series | 360px | Бюджетный Android |

### C. Полезные ссылки

- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [Material Design 3](https://m3.material.io/)
- [WCAG 2.2 Guidelines](https://www.w3.org/WAI/WCAG22/quickref/)

---

## D. Веб-специфика стека

### Технологии проекта

| Технология | Версия | Мобильная релевантность |
|------------|--------|------------------------|
| React | 19 | Виртуальный DOM, reconciliation |
| Tailwind CSS | 3.4 | Responsive breakpoints, утилиты |
| Radix UI | latest | Примитивы без touch targets |
| framer-motion | latest | Анимации (могут быть тяжёлыми) |
| epub.js | 0.3.93 | iframe rendering, touch issues |
| Vite | 6 | HMR, оптимизация бандла |

### Уже реализованные мобильные оптимизации

```
✅ .touch-target утилита (44×44px)     → tailwind.config.js
✅ Safe-area утилиты (pb-safe, etc.)   → tailwind.config.js
✅ xs: 375px breakpoint               → tailwind.config.js
✅ prefers-reduced-motion             → globals.css
✅ touch-action-manipulation          → button.tsx
✅ -webkit-font-smoothing             → globals.css
✅ z-index scale (100-800)            → globals.css
✅ Reader themes (4 варианта)         → globals.css
```

### Проблемы специфичные для веб-приложения

1. **Hover-only UI** (ImageGallery)
   - В нативных приложениях: long-press → context menu
   - В веб: нужен `@media (hover: none)` или JS-детект

2. **Touch targets (Radix UI)**
   - Radix не задаёт минимальные размеры
   - Нужно явно добавлять `.touch-target` или `min-h-[44px]`

3. **Container padding**
   - Tailwind container: 32px padding на всех breakpoints
   - Нужно: 16px на мобилях, 32px на десктопе

4. **iframe (epub.js)**
   - Touch события требуют пробрасывания
   - `-webkit-overflow-scrolling: touch` для iOS

### Рекомендуемые CSS-дополнения

```css
/* globals.css */

/* Убрать синюю подсветку на Android */
* { -webkit-tap-highlight-color: transparent; }

/* Предотвратить pull-to-refresh в модалках */
.modal-scrollable { overscroll-behavior: contain; }

/* Hover-only стили */
@media (hover: hover) and (pointer: fine) {
  .hover-desktop:hover { /* только для мыши */ }
}

/* Touch-visible элементы */
@media (hover: none) and (pointer: coarse) {
  .touch-visible { opacity: 1 !important; }
}
```

### Различия веб vs нативные приложения

| Аспект | Нативное | Веб (fancai) |
|--------|----------|--------------|
| Touch targets | Системные | Требуют настройки |
| Gestures | UIGestureRecognizer | touch events + Hammer.js |
| Safe areas | Автоматически | CSS env() |
| Pull-to-refresh | Системный | Нужно отключать |
| Back button | Hardware | Browser history |
| Hover states | Нет hover | Нужен @media (hover) |
| Animations | 60fps гарантировано | Зависит от CSS |

### Приоритет исправлений с учётом веб-специфики

1. **Критично для веб:**
   - ImageGallery hover → touch-visible (нет аналога в нативных)
   - Container padding (CSS-специфика)

2. **Общие (как в нативных):**
   - Touch targets (44×44px)
   - Safe areas
   - Navigation consistency

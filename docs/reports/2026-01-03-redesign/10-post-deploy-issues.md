# Отчёт о проблемах после редизайна

**Дата:** 3 января 2026
**Статус:** ✅ ВСЕ ПРОБЛЕМЫ ИСПРАВЛЕНЫ

---

## Резюме

~~После деплоя редизайна выявлено **24+ критических и важных проблемы** в мобильной и десктоп версиях.~~

✅ **ВСЕ ПРОБЛЕМЫ ИСПРАВЛЕНЫ** - см. [12-hotfix-completion.md](./12-hotfix-completion.md)

Исправленные категории:

1. **Reader** - ✅ 8 проблем (свайпы, шапка, tap zones)
2. **Локализация** - ✅ 50+ строк переведено на русский
3. **HomePage** - ✅ обложки книг отображаются
4. **Navigation** - ✅ меню и dropdowns работают

---

## I. КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Пролистывание нескольких страниц сразу (Reader)

**Причина:** Двойная обработка свайпов

| Источник | Файл | Строки |
|----------|------|--------|
| useTouchNavigation hook | `useTouchNavigation.ts` | 49-119 |
| Inline handlers в tap zones | `EpubReader.tsx` | 748-784 |
| handleTapZone fallback | `EpubReader.tsx` | 821-834 |

**Проблема:** Три независимых механизма вызывают `nextPage()/prevPage()` одновременно.

**Исправление:**
```tsx
// Оставить ТОЛЬКО useTouchNavigation, убрать inline handlers
// Или наоборот - полностью отключить hook
useTouchNavigation({
  enabled: false, // Отключить hook
});
```

---

### 2. Невозможно кликнуть по описанию в книге

**Причина:** Tap zones перекрывают iframe с контентом

| Проблема | Файл | Строки |
|----------|------|--------|
| z-index: 5 на tap zones | `EpubReader.tsx` | 812, 842, 866 |
| pointerEvents: 'auto' | `EpubReader.tsx` | 815 |

**Исправление:**
```tsx
// Уменьшить z-index и использовать pointer-events: none
<div className="... z-[3] ..." style={{ pointerEvents: 'none' }}>
  <div style={{ pointerEvents: 'auto' }} onClick={...} />
</div>
```

---

### 3. Пустое пространство вместо шапки (Reader)

**Причина:** Несогласованность позиционирования

| Проблема | Файл | Строки |
|----------|------|--------|
| absolute + translate-y-full | `EpubReader.tsx` | 999-1002 |
| paddingTop изменяется | `EpubReader.tsx` | 791-804 |

**Исправление:**
```tsx
// Использовать fixed вместо absolute для ReaderHeader
// Убрать translate animation, оставить только opacity
className={isImmersive ? 'opacity-0 pointer-events-none' : 'opacity-100'}
```

---

### 4. Шапка не отображается на Desktop (Reader)

**Причина:** `isImmersive = true` по умолчанию, paddingTop не учитывает breakpoint

| Проблема | Файл | Строки |
|----------|------|--------|
| isImmersive = true | `EpubReader.tsx` | 140 |
| Нет md: variant для padding | `EpubReader.tsx` | 797 |

**Исправление:**
```tsx
// Добавить responsive padding
className={cn(
  isImmersive ? "pt-[env(safe-area-inset-top)]" : "pt-[calc(70px+env(safe-area-inset-top))]",
  "md:pt-[calc(70px+env(safe-area-inset-top))]" // Всегда на desktop
)}
```

---

### 5. Обложки книг не отображаются (HomePage)

**Причина:** Используется placeholder вместо AuthenticatedImage

| Секция | Файл | Строки |
|--------|------|--------|
| ContinueReadingCard | `HomePage.tsx` | 284-294 |
| RecentBooksSection | `HomePage.tsx` | 437-448 |

**Исправление:**
```tsx
// Заменить placeholder на AuthenticatedImage
import { AuthenticatedImage } from '@/components/UI/AuthenticatedImage';

{book.has_cover ? (
  <AuthenticatedImage
    src={`${import.meta.env.VITE_API_URL}/books/${book.id}/cover`}
    alt={`${book.title} обложка`}
    className="w-full h-full object-cover"
    fallback={<BookOpen className="..." />}
  />
) : (
  <BookOpen className="..." />
)}
```

---

### 6. "nav.library" вместо "Моя библиотека" (Header)

**Причина:** Отсутствует ключ в локализации

| Проблема | Файл | Строки |
|----------|------|--------|
| `t('nav.library')` без перевода | `Header.tsx` | 72 |

**Исправление:**
```tsx
// Вариант A: Изменить ключ
{ to: '/library', label: t('nav.myLibrary'), icon: Library }

// Вариант B: Добавить ключ в ru.ts
nav: {
  library: 'Моя библиотека',
}
```

---

### 7. Не работает мобильное меню (Header)

**Причина:** `onMenuClick` не связан с store

| Проблема | Файл | Строки |
|----------|------|--------|
| onClick={onMenuClick} | `Header.tsx` | 92 |

**Исправление:**
```tsx
import { useUIStore } from '@/stores/ui';
const { setSidebarOpen, sidebarOpen } = useUIStore();
// ...
onClick={() => setSidebarOpen(!sidebarOpen)}
```

---

## II. ВАЖНЫЕ ПРОБЛЕМЫ

### 8. Прозрачный dropdown аватарки

| Файл | Строки | Проблема |
|------|--------|----------|
| `Header.tsx` | 179 | `bg-popover/95` - полупрозрачный |

**Исправление:** `bg-popover` или `bg-background`

---

### 9. Dropdown темы смещается влево

| Файл | Строки | Проблема |
|------|--------|----------|
| `ThemeSwitcher.tsx` | 54 | Некорректный align |

**Исправление:** Добавить `alignOffset={-8}` или `side="bottom"`

---

### 10. Viewport meta tag неполный

| Файл | Строки | Проблема |
|------|--------|----------|
| `index.html` | 6 | Нет maximum-scale, viewport-fit |

**Исправление:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
```

---

### 11. snap-mandatory в горизонтальном скролле

| Файл | Строки | Проблема |
|------|--------|----------|
| `HomePage.tsx` | 414 | Скролл "застревает" |

**Исправление:** `snap-proximity` вместо `snap-mandatory`

---

### 12. Разные timeThreshold для свайпов

| Файл | Значение |
|------|----------|
| `useTouchNavigation.ts:46` | 300ms |
| `EpubReader.tsx:766` | 500ms |

**Исправление:** Унифицировать до 300ms

---

## III. ЛОКАЛИЗАЦИЯ (50+ строк)

### Приоритет HIGH

| Файл | Количество строк |
|------|------------------|
| `LibraryPage.tsx` | 30+ |
| `BookCard.tsx` | 6 |
| `BookGrid.tsx` | 6 |

### Конкретные строки для перевода

**LibraryPage.tsx:**
| Строка | English | Русский |
|--------|---------|---------|
| 50 | "Newest First" | "Сначала новые" |
| 51 | "Oldest First" | "Сначала старые" |
| 52 | "Title A-Z" | "По названию А-Я" |
| 53 | "Title Z-A" | "По названию Я-А" |
| 54 | "Author A-Z" | "По автору А-Я" |
| 55 | "Recently Read" | "Недавно читал" |
| 60 | "All Genres" | "Все жанры" |
| 61 | "Fiction" | "Художественная" |
| 62 | "Non-Fiction" | "Документальная" |
| 63 | "Fantasy" | "Фэнтези" |
| 64 | "Sci-Fi" | "Научная фантастика" |
| 65 | "Romance" | "Романтика" |
| 66 | "Mystery" | "Детектив" |
| 67 | "Thriller" | "Триллер" |
| 72 | "All Books" | "Все книги" |
| 73 | "Not Started" | "Не начаты" |
| 74 | "In Progress" | "В процессе" |
| 75 | "Completed" | "Завершены" |
| 256 | "Upload Book" | "Загрузить книгу" |
| 269 | "Search by title or author..." | "Поиск по названию или автору..." |
| 362 | "Filters" | "Фильтры" |
| 403 | "Reading Progress" | "Прогресс чтения" |
| 434 | "Clear all filters" | "Сбросить фильтры" |
| 475 | "Previous" | "Назад" |
| 485 | "Next" | "Далее" |

**BookCard.tsx:**
| Строка | English | Русский |
|--------|---------|---------|
| 168 | "AI Processing..." | "AI обработка..." |
| 191 | "Read" | "Читать" |
| 203 | "Delete" | "Удалить" |
| 245 | "Read" | "Читать" |
| 254 | "Delete" | "Удалить" |
| 262 | "Close" | "Закрыть" |

**BookGrid.tsx:**
| Строка | English | Русский |
|--------|---------|---------|
| 138 | "No books found" | "Книги не найдены" |
| 150 | "Clear Search" | "Очистить поиск" |
| 168 | "Your library is empty" | "Ваша библиотека пуста" |
| 181 | "Upload First Book" | "Загрузить первую книгу" |

**ImageGallery.tsx:**
| Строка | English | Русский |
|--------|---------|---------|
| 130 | "Loading images..." | "Загрузка изображений..." |
| 139 | "Failed to Load Images" | "Не удалось загрузить" |
| 152 | "No Images Generated Yet" | "Изображений пока нет" |
| 167 | "Generated Images" | "Созданные изображения" |
| 206 | "All Types" | "Все типы" |

---

## IV. СВОДНАЯ ТАБЛИЦА ПРОБЛЕМ

| # | Проблема | Критичность | Файл |
|---|----------|-------------|------|
| 1 | Пролистывание нескольких страниц | 🔴 CRITICAL | EpubReader.tsx |
| 2 | Клик по описанию не работает | 🔴 CRITICAL | EpubReader.tsx |
| 3 | Пустое пространство вместо шапки | 🔴 CRITICAL | EpubReader.tsx |
| 4 | Шапка не видна на Desktop | 🔴 CRITICAL | EpubReader.tsx |
| 5 | Обложки не отображаются | 🔴 CRITICAL | HomePage.tsx |
| 6 | nav.library вместо перевода | 🔴 CRITICAL | Header.tsx |
| 7 | Мобильное меню не работает | 🔴 CRITICAL | Header.tsx |
| 8 | Прозрачный dropdown | 🟠 HIGH | Header.tsx |
| 9 | Dropdown темы смещается | 🟠 HIGH | ThemeSwitcher.tsx |
| 10 | Viewport meta неполный | 🟠 HIGH | index.html |
| 11 | snap-mandatory | 🟡 MEDIUM | HomePage.tsx |
| 12 | Разные timeThreshold | 🟡 MEDIUM | Multiple |
| 13-24 | Локализация (50+ строк) | 🟠 HIGH | Multiple |

---

## V. ПЛАН ИСПРАВЛЕНИЙ

### Этап 1: Критические (Reader) - 4 часа

1. Отключить дублирование свайп-обработчиков
2. Исправить z-index tap zones
3. Исправить позиционирование шапки Reader
4. Добавить responsive padding для desktop
5. Добавить debounce на навигацию

### Этап 2: Критические (UI) - 2 часа

6. Добавить AuthenticatedImage в HomePage
7. Исправить nav.library ключ
8. Подключить мобильное меню к store
9. Исправить прозрачность dropdown

### Этап 3: Важные - 2 часа

10. Обновить viewport meta tag
11. Исправить dropdown alignment
12. Унифицировать threshold параметры
13. Заменить snap-mandatory

### Этап 4: Локализация - 3 часа

14. LibraryPage.tsx - 30+ строк
15. BookCard.tsx - 6 строк
16. BookGrid.tsx - 6 строк
17. ImageGallery.tsx - 15 строк
18. Другие компоненты

---

## Связанные документы

- [09-phase6-completion.md](./09-phase6-completion.md)
- [01-redesign-master-plan.md](./01-redesign-master-plan.md)
